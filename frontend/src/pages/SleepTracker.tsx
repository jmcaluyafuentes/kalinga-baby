import { Container } from '@mui/material';
import ComingSoon from '../components/ComingSoon';

const SleepTracker = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <ComingSoon title="Sleep Tracker" />
    </Container>
  );
};

export default SleepTracker;
