import { Container } from '@mui/material';
import ComingSoon from '../components/ComingSoon';

const Milestones = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <ComingSoon title="Milestones" />
    </Container>
  );
};

export default Milestones;
