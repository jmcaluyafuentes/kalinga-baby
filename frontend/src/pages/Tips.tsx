import { Container } from '@mui/material';
import ComingSoon from '../components/ComingSoon';

const Tips = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <ComingSoon title="Tips" />
    </Container>
  );
};

export default Tips;
